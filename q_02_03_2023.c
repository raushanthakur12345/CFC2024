//Write a C program to calculate factorial of an integer number taking input from thecommand line.
#include<stdio.h>
int main(int argc, char *argv[]){
	int i, s;
	//x = atoi(argv[1]); 
	//y = atoi(argv[2]);
	s=s*i;
	printf("\n Sum = %d",s);
	return 0;
}
